<ui:FluentWindow x:Class="MacroPanels.View.EditPanelWindow"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
xmlns:local="clr-namespace:MacroPanels.View"
mc:Ignorable="d"
Title="Edit Command"
Width="700"
MinWidth="500" MinHeight="300"
MaxHeight="1000"
SizeToContent="Height"
WindowStartupLocation="CenterOwner"
ExtendsContentIntoTitleBar="True"
WindowBackdropType="Mica"
WindowCornerPreference="Round">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Edit Command" ShowMaximize="True" ShowMinimize="True"/>

        <ui:Card Grid.Row="1" Margin="8,8,8,4" Padding="0">
            <local:EditPanel x:Name="EditPanelContent"/>
        </ui:Card>

        <!-- OK/Cancel Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="8,4,8,8">
            <ui:Button Content="OK" Appearance="Primary" Width="100" Margin="0,0,8,0" Click="OkButton_Click"/>
            <ui:Button Content="Cancel" Width="100" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
