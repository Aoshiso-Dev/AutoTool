<UserControl x:Class="AutoTool.Desktop.Panels.View.LogPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:shared="clr-namespace:AutoTool.Desktop.Panels.ViewModel.Shared"
             mc:Ignorable="d"
             d:DesignHeight="220" d:DesignWidth="800">

    <Grid Background="{ui:ThemeResource SolidBackgroundFillColorBaseBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Height="30"
                     VerticalContentAlignment="Center"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="ログを検索"/>

            <ui:Button Grid.Column="1"
                       Content="コピー"
                       Command="{Binding CopyLogsCommand}"
                       Height="30"
                       Margin="8,0,0,0"
                       Appearance="Secondary"/>

            <ui:Button Grid.Column="2"
                       Content="クリア"
                       Command="{Binding ClearLogsCommand}"
                       Height="30"
                       Margin="8,0,0,0"
                       Appearance="Secondary"/>
        </Grid>

        <ListBox x:Name="LogListBox"
                 Grid.Row="1"
                 ItemsSource="{Binding FilteredLogEntries}"
                 BorderThickness="0"
                 Background="{ui:ThemeResource SolidBackgroundFillColorBaseBrush}"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 FontFamily="Cascadia Code, Consolas, Courier New"
                 FontSize="12">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,0,0,2"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Focusable" Value="False"/>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type shared:LogEntry}">
                    <Border Padding="6,4"
                            CornerRadius="4"
                            BorderThickness="2,0,0,0"
                            BorderBrush="#5B5B5B"
                            Background="{ui:ThemeResource ControlFillColorDefaultBrush}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="138"/>
                                <ColumnDefinition Width="44"/>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Text="{Binding Timestamp}"
                                       Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Grid.Column="1"
                                       Text="{Binding LineNumber}"
                                       Margin="8,0,0,0"
                                       Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Grid.Column="2"
                                       Text="{Binding CommandName}"
                                       Margin="8,0,0,0"
                                       Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            <TextBlock Grid.Column="3"
                                       Text="{Binding Message}"
                                       Margin="8,0,0,0"
                                       TextWrapping="Wrap"
                                       Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                        </Grid>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Level}" Value="{x:Static shared:LogEntryLevel.Start}">
                                        <Setter Property="BorderBrush" Value="#3B82F6"/>
                                        <Setter Property="Background" Value="#1F3B82F6"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Level}" Value="{x:Static shared:LogEntryLevel.Success}">
                                        <Setter Property="BorderBrush" Value="#10B981"/>
                                        <Setter Property="Background" Value="#1F10B981"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Level}" Value="{x:Static shared:LogEntryLevel.Warning}">
                                        <Setter Property="BorderBrush" Value="#F59E0B"/>
                                        <Setter Property="Background" Value="#1FF59E0B"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Level}" Value="{x:Static shared:LogEntryLevel.Error}">
                                        <Setter Property="BorderBrush" Value="#EF4444"/>
                                        <Setter Property="Background" Value="#1FEF4444"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>

