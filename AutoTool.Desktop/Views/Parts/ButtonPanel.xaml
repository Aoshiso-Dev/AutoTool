<UserControl x:Class="AutoTool.Desktop.Views.Parts.ButtonPanel"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                mc:Ignorable="d"
                xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit">
    <Border Padding="8" Background="#FAFAFB" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
        <DockPanel>
            <!-- Left group: file -->
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button Content="New" Command="{Binding NewCommand}"/>
                <Button Content="Load" Command="{Binding LoadCommand}"/>
                <Button Content="Save" Command="{Binding SaveCommand}"/>
            </StackPanel>


            <!-- Center group: edit -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Add" Command="{Binding AddCommand}"/>
                <Button Content="Remove" Command="{Binding RemoveCommand}"/>
                <Button Content="Up" Command="{Binding MoveUpCommand}"/>
                <Button Content="Down" Command="{Binding MoveDownCommand}"/>
            </StackPanel>


            <!-- Right group: run -->
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <xctk:WatermarkTextBox Width="200" Watermark="Search..." Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Run" Background="{StaticResource AccentBrush}" Foreground="White" Command="{Binding RunCommand}"/>
                <Button Content="Stop" Command="{Binding StopCommand}"/>
            </StackPanel>
        </DockPanel>
    </Border>
</UserControl>