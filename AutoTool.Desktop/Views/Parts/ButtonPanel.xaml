<UserControl x:Class="AutoTool.Desktop.Views.Parts.ButtonPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:AutoTool.Desktop.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ButtonPanelViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            
        </StackPanel>
        
        <!-- Main Button Bar -->
        <ToolBar>
            <ToggleButton IsChecked="{Binding IsRunning, Mode=TwoWay}"
              Command="{Binding RunCommand}"
              Padding="6,2"
              Width="100"
              Margin="3">
                <ToggleButton.Style>
                    <Style TargetType="ToggleButton">
                        <Setter Property="Background" Value="Green"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderBrush" Value="DarkGreen"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Content" Value="実行"/>
                        <Style.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="Red"/>
                                <Setter Property="BorderBrush" Value="DarkRed"/>
                                <Setter Property="Content" Value="停止"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ToggleButton.Style>
            </ToggleButton>
            <Border Width="1" Background="#E5E7EB"/>
            <Button Content="新規" Command="{Binding NewCommand}" Margin="3"/>
            <Border Width="1" Background="#E5E7EB"/>
            <Button Content="開く" Command="{Binding LoadCommand}" Margin="3"/>
            <Button Content="保存" Command="{Binding SaveCommand}" Margin="3"/>
            <Border Width="1" Background="#E5E7EB"/>
            <Button Content="元に戻す" Command="{Binding UndoCommand}" Margin="3"/>
            <Button Content="やり直し" Command="{Binding RedoCommand}" Margin="3"/>
            <Border Width="1" Background="#E5E7EB"/>
            <Border Width="1" Background="#E5E7EB"/>
            <TextBlock Text="コマンド:" VerticalAlignment="Center" Margin="3"/>
            <ComboBox ItemsSource="{Binding AvailableCommands}" SelectedItem="{Binding SelectedCommand}" DisplayMemberPath="DisplayName" VerticalAlignment="Center" Width="200"  Margin="3"/>
            <Button Content="追加" Command="{Binding AddCommand}" Margin="3"/>
            <Button Content="削除" Command="{Binding RemoveCommand}" Margin="3"/>
            <Border Width="1" Background="#E5E7EB" Margin="6,0"/>
            <Button Content="上へ" Command="{Binding MoveUpCommand}" Margin="3"/>
            <Button Content="下へ" Command="{Binding MoveDownCommand}" Margin="3"/>
        </ToolBar>
    </Grid>
</UserControl>