<UserControl x:Class="AutoTool.Desktop.Views.Parts.CommandPalette"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            mc:Ignorable="d"
            xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit">
    <Border Padding="8" Background="#FFFFFF" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>


            <xctk:WatermarkTextBox Grid.Row="0" Watermark="コマンドを検索..."
                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>


            <ListBox Grid.Row="1" ItemsSource="{Binding FilteredDescriptors}" SelectedItem="{Binding SelectedDescriptor}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="4">
                            <TextBlock Text="{Binding DisplayName}"/>
                            <TextBlock Text="  (" Foreground="#9CA3AF"/>
                            <TextBlock Text="{Binding Type}" Foreground="#9CA3AF"/>
                            <TextBlock Text=")" Foreground="#9CA3AF"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>


            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="キャンセル" Command="{Binding CancelCommand}"/>
                <Button Content="追加" Background="{StaticResource AccentBrush}" Foreground="White" Command="{Binding CreateCommand}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>