<UserControl x:Class="AutoTool.Desktop.Views.Parts.EditPanel"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
            xmlns:ifcmd="clr-namespace:AutoTool.Commands.Flow.If;assembly=AutoTool.Commands"
            xmlns:whilecmd="clr-namespace:AutoTool.Commands.Flow.While;assembly=AutoTool.Commands"
            mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <TextBlock Text="Editor" FontSize="14" FontWeight="SemiBold" Margin="8,8,8,4"/>


        <Border Grid.Row="1" Margin="8" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8" Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>


                <!-- 汎用：PropertyGrid に EditorVM を束ねる（SelectedEditor は VM 側で用意） -->
                <xctk:PropertyGrid Grid.Row="0"
                                    Name="PropertyGrid"
                                    SelectedObject="{Binding SelectedEditor}"
                                    AutoGenerateProperties="True"/>


                <!-- 適用/リセット（EditorVMが ICommand を提供する前提） -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Revert" Command="{Binding SelectedEditor.RevertCommand}"/>
                    <Button Content="Apply" Background="{StaticResource AccentBrush}" Foreground="White"
                            Command="{Binding SelectedEditor.ApplyCommand}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>