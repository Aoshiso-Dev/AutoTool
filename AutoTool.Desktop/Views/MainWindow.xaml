<Window x:Class="AutoTool.Desktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AutoTool.Desktop.ViewModels"
        mc:Ignorable="d"
        Title="AutoTool" Height="720" Width="1200"
        Background="#FFF"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="700"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Top Buttons: DIで解決した View をここに置く -->
        <ContentControl x:Name="ButtonHost" Grid.Row="0" Grid.ColumnSpan="2"/>

        <!-- Left: List -->
        <ContentControl x:Name="ListHost" Grid.Row="1" Grid.Column="0"/>

        <!-- Right: Editor -->
        <ContentControl x:Name="EditHost" Grid.Row="1" Grid.Column="1"/>
    </Grid>
</Window>