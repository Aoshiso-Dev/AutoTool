<Window x:Class="AutoTool.Desktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:AutoTool.Desktop.Views"
        xmlns:parts="clr-namespace:AutoTool.Desktop.Views.Parts"
        xmlns:vm="clr-namespace:AutoTool.Desktop.ViewModels"
        mc:Ignorable="d"
        Title="AutoTool - 自動操作ツール" Height="720" Width="1200"
        Background="#FFF"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="420"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Top Buttons -->
        <parts:ButtonPanel x:Name="ButtonPanel" Grid.Row="0" Grid.ColumnSpan="2"/>


        <!-- Left: Tree/List -->
        <parts:ListPanel Grid.Row="1" Grid.Column="0"/>


        <!-- Right: Editor -->
        <parts:EditPanel x:Name="EditPanel" Grid.Row="1" Grid.Column="1"/>
    </Grid>
</Window>